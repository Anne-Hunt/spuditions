version: '3'
services:
  app:
    image: raccoonsynthesizer/spuditions:latest
    restart: unless-stopped
    env_file:
      - spuditions-env
    ports:
      - "3000:3000"
  JWTRS256_KEY:
    file: server/jwtRS256.key
  JWTRS256_KEY_PUB:
    file: server/jwtRS256.key.pub
